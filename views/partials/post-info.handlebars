 <article class="post">
      <div class="title">
        <a href="/post/{{id}}" target="_blank" >{{title}}</a>
        <span class="justify">(Posted by {{user.username}} on {{created_at}})</span>
      </div>
      <div class="meta">
        {{posted_note}}
        <a href="/post/{{id}}" class="justifyComment border-bottom">{{comments.length}} comment(s)</a>
      </div>
    </article>