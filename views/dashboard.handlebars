    <a href="/" class="d-flex justify-content-end">&larr; Back to Homepage</a>

<section class="card">
    <div class="card-body">
    <h2 class="card-header">Create New Post</h2>
    <form class="create-new-post">
        <div class="form-group">
            <label for="post-title" class="card-title font-weight-bold mt-3">Title:</label>
            <input type="text" id="post-title" name="post-title">
        </div>

        <div>
            <label for="posted-note" class="card-title font-weight-bold">Create Your new Posted Note Here:</label>
            <textarea class="form-control" name="posted-note"></textarea>
        </div>
        <button type="submit" class="btn btn-primary">Create Post</button>
    </form>

    </div>
</section>


{{#if posts.length}}
<section class=" mt-5 card border-top">
    <div class="card-body">

    <h2 class="mt-3 mb-3 card-header">Your Posts</h2>
    
        {{#each posts as |post|}}
      
            {{> post-info post}}
            <a href="/dashboard/edit/{{post.id}}" class="edit">Edit Post</a>
        
        {{/each}}
    </div>
  
</section>
{{/if}}


{{!-- import javascript from create-post --}}
<script src="/javascript/create-post.js"></script>

